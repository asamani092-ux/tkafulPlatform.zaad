# UX2 — تقرير المرحلة 5: التدقيق العرضي والاتساق

**الفرع:** `fix/ux2-sweep` · **الأساس:** `fix/admin-domains-v2`

## الكنس العرضي (المشكلات الأربع)
- **#1 حقول بلا تسمية / #2 تسميات مبهمة:** أُضيف `FileInput` معنون واستُخدم في
  بوابتَي المورّد والمندوب (كانت `<input type=file>` بتسمية غير مرتبطة). حارس
  `dsAdoption.test.ts` وُسّع ليشمل **الإدارة + السقيا + الأصداف**، ويقبل الوسم
  الخام فقط إذا كان معنوناً (`aria-label`) أو `checkbox` عبر غلاف.
- **#3 بطاقات/لوحات مضغوطة افتراضياً:** أغلفة `Panel`/`CompactListCard` والبطاقات
  المختصرة عبر النطاقات (المرحلة 1 + 4).
- **#4 لا تنقّل بإعادة تحميل:** لا `window.location.href` داخلي (المرحلة 2).

## تدقيق الخصوصية
- بوّابة التقارير وتقرير المتطوّع: `IsAdmin` حصراً؛ لا نقاط عامة تُظهر PII.
- رفع الخرائط بالجملة: إحداثيات/أسماء مواقع فقط؛ الحقول المخصّصة مقصورة على
  مخطط `MapItemField`؛ محمي بنطاق كتابة المشروع.
- نطاق الكفالات بالمشروع يضيّق الرؤية فقط (لا يوسّعها) فوق فلتر الدور IDOR-safe.
- إخفاء `<5` يبقى في التجميعات العامة للخرائط (لا يمسّه التغيير).
- تفاصيل موثّقة في `PERMISSION_TABLE.md` (قسم UX2).

## جدول الصلاحيات
`PERMISSION_TABLE.md` — قسم **UX2** يغطّي النقاط الجديدة/المعدّلة:
`reports/scope`, `volunteer-tasks(+metrics)`, `maps items/bulk_upload+template`,
`projects/add_member` (أي مستخدم)، وتصفية الكفالات `?project=`.

## مصدر الحقيقة للجولة القادمة
`FEATURE_MATRIX.md` محدّث لكل النطاقات بعد UX2 (بحث الأعضاء، رفع الخرائط بالجملة،
بوّابة التقارير، الكفالات المُنطّقة، توحيد الأدوار).

## البوّابة (Gate)
- ✅ Vitest: **100/100** · الخلفية: **266** `OK (skipped=1)`.
- ✅ `tsc && vite build` أخضر.
- ✅ `check --deploy`: لا أخطاء برمجية — التحذيرات الستة إعدادات نشر قياسية
  (HSTS/SSL/SECRET_KEY/DEBUG/كوكيز) تُضبط عبر متغيّرات البيئة في الإنتاج، وهي
  ناتجة عن `.env` المحلي (DEBUG=True) لا عن هذا العمل.

## خارج النطاق (الجولة القادمة)
البنود الـ23 غير المُختبَرة: دورة الكفالات 5.x، تدفّقات الخرائط 6.x، سلامة البيانات 7.x.
