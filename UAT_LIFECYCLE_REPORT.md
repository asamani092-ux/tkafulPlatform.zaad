# تقرير تجربة المستخدم الشامل — دورة حياة الأدوار

**التاريخ:** 2026-09-04  
**البيئة:** محلية بعد دمج `main` عند الالتزام `c345990`  
**طلب السحب:** [#15](https://github.com/asamani092-ux/tkafulPlatform.zaad/pull/15) — **مُدمَج**  
**النشر على الخادم البعيد:** غير مُنفَّذ من هذه البيئة (لا وصول لجهاز الإنتاج / لا مفاتيح نشر). الكود على `main` محدَّث؛ تشغيل الإنتاج يتطلب `./deploy/deploy.sh` على الخادم.

---

## 1) ملخص تنفيذي

دُمجت منظومة التحكم بالكفالات في `main` وشُغّلت محلياً. اختُبرت الأدوار التالية كمستخدم نهائي (واجهة + واجهة برمجة): زائر، مشرف، مدير، موظف، متبرّع، مورّد، مندوب، متطوّع، مستفيد.

| المحور | الحكم |
|--------|--------|
| دورة الكفالة API (إنشاء → موافقة → إسناد → تحضير → تسليم → اكتمال) | تعمل |
| حماية تجاوز التمويل / صلاحيات الإدارة | تعمل في الغالب |
| تجربة ما بعد تسجيل الدخول لغير المتطوع | ضعيفة: الجميع يُرمى تقريباً إلى لوحة متطوع |
| بوابة المستفيد | غير موجودة كتجربة مستقلة |
| اكتمال بيانات الكادر (مدير/موظف) | فارغة في البيئة التجريبية |

---

## 2) حالة الدمج والنشر

1. دُمج فرع `cursor/sponsorship-control-system-069a` في `main` (fast-forward إلى `c345990`).
2. طلب السحب #15 بحالة **MERGED**.
3. الخدمات المحلية: Django `:8000` + Vite `:3000` على آخر `main`.
4. **فجوة نشر:** لا يوجد في هذه الجلسة اتصال بخادم Ubuntu/VPS لتشغيل `deploy/deploy.sh` أو إعادة تشغيل gunicorn. يجب تنفيذ ذلك يدوياً على الإنتاج.

---

## 3) منهجية الاختبار

- حسابات تجريبية لكل دور (بريد `*@test.local` / كلمة المرور `Test12345!`، والمشرف `admin@takaful.com` / `admin123`).
- دورة كفالة كاملة عبر `/api/saqya/`.
- مسح صلاحيات GET لكل دور.
- جولة واجهة عبر المتصفح مع حقن الجلسة (لتجاوز عطل أتمتة حقول الدخول) ولقطات شاشة تحت `/opt/cursor/artifacts/`.

---

## 4) دورة حياة كل دور — ماذا حدث كمستخدم؟

### 4.1 الزائر (بدون حساب)

**ما نجح**
- الصفحة الرئيسية، المشاريع، صفحة مشروع السقيا، المتطوعون، الخدمات، من نحن، الخريطة — كلها متاحة علناً.
- بوابة الكفالات تطلب تسجيل الدخول برسالة عربية واضحة.

**مشاكل واجهتها**
- أرقام الواجهة العامة ضعيفة المعنى أحياناً (`0 + مستفيد` بينما المشاريع موجودة) — ثقة المستخدم بالمنصة تتأثر.
- لا مسار واضح من الصفحة الرئيسية إلى «أريد أكفل الآن» دون تخمين مسار المشروع.

**تحسينات مقترحة**
- بطاقة CTA للكفالة من الرئيسية مباشرة إلى `/projects/saqya/sponsorships`.
- مزامنة إحصاءات الواجهة مع بيانات حقيقية أو إخفاؤها إن كانت صفراً.

### 4.2 المشرف العام (`admin`)

**ما نجح**
- لوحة `/Admin` غنية: مشاريع، مستخدمون، متطوعون، نماذج طلبات، خرائط، تقارير، إعدادات، مصفوفة أدوار.
- إدارة كفالات المشروع `/Admin/projects/saqya/sponsorships` تعرض ملخصاً وتبويباً للأنواع.
- من API: اعتماد كفالة، إنشاء طلب، إسناد مورّد/مندوب، إكمال الطلب بعد التسليم.

**مشاكل واجهتها**
- كثافة الشريط الجانبي عالية؛ المهمة التالية غير موجَّهة (لا قائمة «يحتاج إجراءك»).
- إعدادات أنواع الكفالات موجودة لكن اكتشافها من داخل لوحة الكفالات أقل وضوحاً من تبويب صريح دائم.
- لا يظهر بسهولة نطاق المورّدين/المندوبين المسموحين أثناء الإسناد في الواجهة (الفرض موجود خادماً).

**تحسينات مقترحة**
- صندوق «إجراءات معلّقة»: كفالات بانتظار المراجعة، طلبات بلا إسناد، تسليمات بلا توثيق.
- في نموذج الإسناد: قائمة منسدلة مفلترة حسب قائمة المشروع المسموحة مع رسالة إن كانت فارغة (= بلا قيد).

### 4.3 المدير (`manager`) والموظف (`employee`)

**ما نجح**
- الوصول إلى `/Admin/staff` يعمل.
- منع الوصول لـ `/Admin/projects` و`/Admin/users` برسالة 403 عربية — صحيح أمنياً.

**مشاكل واجهتها**
- شاشة الكادر فارغة بالكامل («لا توجد بيانات كادر») مع توجيه لتغذية اللوحة — شعور بأن الدور غير مفعّل.
- عند زيارة `/user/main` تظهر **إحصائيات متطوع** لنفس الحساب — مربك جداً لمدير/موظف.
- لا مسار واضح من الكادر إلى مشاريع الكفالات حتى للقراءة إن كان عضواً في مشروع.

**تحسينات مقترحة**
- توجيه ما بعد الدخول حسب الدور إلى `/Admin/staff` دون عرض هوية متطوع.
- بذرة بيانات تجريبية للكادر أو معالج إعداد أول مرة.
- إن وُجدت عضوية مشروع: إظهار رابط «مشاريعي» بدل 403 صامت على كل شيء عدا الكادر.

### 4.4 المتبرّع (`donor`)

**دورة الحياة المختبرة**
1. إنشاء كفالة مع نوع ديناميكي و`type_data` عبر API — نجح.
2. الواجهة `/projects/saqya/sponsorships` تعرض نموذج إنشاء + قائمة كفالاته — نجح.
3. لا يستطيع اعتماد كفالته بنفسه — صحيح.
4. بعد الاعتماد يظهر مسار التمويل/الطلبات عبر دورة التشغيل.

**مشاكل واجهتها**
- بعد تسجيل الدخول الافتراضي يُوجَّه إلى `/user/main` (لوحة متطوع: ساعات، نقاط، مهام) **بدل بوابة الكفالات** — هذا أكبر احتكاك في الرحلة.
- لا يتضح الفرق بين «إنشاء كفالة» و«تمويل/متابعة الدفع» في الواجهة بنفس وضوح API (`pay` / `checkout_url`).
- الحقول الديناميكية للنوع تظهر في الإنشاء، لكن المعاينة بعد الإرسال للمستخدم غير بارزة كبطاقة تفاصيل.

**تحسينات مقترحة**
- توجيه ما بعد الدخول: `donor` → `/projects/saqya/sponsorships` (أو آخر مشروع كفالات نشط).
- خط زمني بسيط لحالة الكفالة (مسودة/مراجعة/معتمدة/مموّلة/قيد التنفيذ/مكتملة).
- إبراز زر الدفع/التمويل عند وجود رصيد متبقٍ.

### 4.5 المورّد (`supplier`)

**ما نجح**
- بوابة المورّد تعرض «طلبات التوريد المسندة» مع حالة وحركة «رفع فاتورة».
- API: `prepare` ثم `ready` يعملان؛ الإشعارات البريدية تُطلق عند الإسناد/الجاهزية (في وضع التطوير تظهر كرسائل محلية).

**مشاكل واجهتها**
- نفس مشكلة الهبوط على لوحة المتطوع بعد الدخول.
- لا خطوات واضحة في الواجهة لأزرار «بدء التحضير» / «جاهز للتسليم» بنفس وضوح الـ API (الاعتماد على رفع فاتورة فقط في الشاشة المختبرة).
- عند اكتمال الطلب يختفي الشعور بالتقدم التاريخي (سجل أحداث للمورّد).

**تحسينات مقترحة**
- أزرار حالة صريحة مطابقة للـ API مع تعطيل الحالات غير المسموحة.
- سجل زمني لكل طلب + مرفقات الفاتورة بحالة مراجعة.

### 4.6 المندوب (`representative`)

**ما نجح**
- بوابة «مهامي الميدانية» مع «رفع توثيق».
- API: `deliver` ينجح؛ الإكمال النهائي من صلاحية أعلى (`complete` مرفوض للمندوب) — منطقي للفصل بين التسليم والإغلاق المالي/الإداري.

**مشاكل واجهتها**
- هبوط على لوحة متطوع بعد الدخول.
- واجهة التوثيق مختصرة؛ لا إرشاد لحقول مطلوبة (صور، موقع، ملاحظات).
- لا خريطة مهمة ميدانية داخل بوابة المندوب رغم وجود أداة خرائط في المنصة.

**تحسينات مقترحة**
- نموذج توثيق منظم (قبل/بعد، إحداثيات، ملاحظة إلزامية).
- ربط نقطة التسليم بخريطة المشروع.

### 4.7 المتطوّع (`user`)

**ما نجح**
- `/user/main` و`/user/tasks` و`/user/settings` تعمل عربية وواضحة الهيكل.
- التسجيل العام موجَّه لمتطوع جديد — متسق مع هذا الدور.

**مشاكل واجهتها**
- البيئة فارغة: لا فرص، لا مهام — المنصة تبدو «ميتة» لمتطوع جديد.
- لا جسر واضح من حساب المتطوع إلى مشروع السقيا إلا عبر التصفح العام.

**تحسينات مقترحة**
- بطاقة «ابدأ هنا»: انضم لمشروع / تصفّح الفرص / أكمل الملف.
- إخفاء أو تخفيف الأصفار الكبيرة واستبدالها بدعوة لإجراء أول.

### 4.8 المستفيد (`beneficiary`)

**ما نجح**
- حظر بوابة الكفالات برسالة: الوحدة متاحة للمتبرّعين والمورّدين والمندوبين والمشرفين — واضح.
- منع دخول `/Admin`.

**مشاكل واجهتها (حرجة كمنتج)**
- بعد الدخول يُعامل كمتطوع بالكامل (ساعات، نقاط، مهام) — **لا بوابة مستفيد**.
- لا مسار لطلب خدمة/مساعدة مرتبط بدوره، رغم وجود نماذج طلبات عامة وخدمات.
- الدور موجود في النظام لكن رحلة المستخدم غير مُصمَّمة.

**تحسينات مقترحة**
- بوابة مستفيد: طلباتي، حالة الطلب، مستنداتي، تواصل.
- توجيه ما بعد الدخول إلى `/services` أو `/user/requests` بدل لوحة المتطوع.
- إمّا إكمال الرحلة أو إخفاء الدور من واجهة التسجيل حتى يجهز.

---

## 5) دورة الكفالة الشاملة (تشغيلية)

| الخطوة | النتيجة |
|--------|---------|
| إنشاء نوع كفالة بحقول ديناميكية | نجاح |
| متبرّع ينشئ كفالة + `type_data` | نجاح |
| مشرف يعتمد | نجاح |
| إنشاء طلب وربطه | نجاح |
| إسناد مورّد/مندوب | نجاح + إشعار |
| مورّد: تحضير → جاهز | نجاح |
| مندوب: تسليم | نجاح |
| إكمال الطلب من صلاحية أعلى | نجاح |
| محاولة تجاوز التمويل / دفع غير مسموح | مرفوض كما هو متوقع |
| قائمة السماح الفارغة = بلا قيد؛ غير الفارغة = فرض | مُختبر سابقاً في مرحلة 2 |

---

## 6) مشاكل أفقية (تخص أكثر من دور)

1. **توجيه ما بعد تسجيل الدخول موحّد خاطئ:** غير المشرف/الكادر يذهب إلى `/user/main` بهوية متطوع حتى لو كان متبرّعاً/مورّداً/مندوباً/مستفيداً.
2. **ازدواج مسارات API:** `/api/saqya/` و`/api/sponsorships/` معاً — يزيد الالتباس على المطوّرين والوكلاء الآليين.
3. **أتمتة نموذج الدخول صعبة** (مكونات مضبوطة) — تعيق الاختبار المستمر؛ يُنصح بـ `data-testid` لحقول الدخول.
4. **الإحصاءات الصفرية** في لوحات متعددة تضعف الثقة.
5. **البريد الإشعاري** يعمل كطابعة محلية في التطوير؛ يحتاج تأكيد إعداد SMTP على الإنتاج بعد النشر.
6. **المشرف يرى كل شيء** بينما المدير/الموظف بلا عضوية مشروع محرومون من أي سياق مشاريع — الفجوة بين «كادر» و«مشروع» واسعة.

---

## 7) أولويات التطوير المقترحة

| الأولوية | البند |
|----------|--------|
| P0 | توجيه ما بعد الدخول حسب الدور (متبرّع/مورّد/مندوب/مستفيد/متطوع/كادر/مشرف) |
| P0 | بوابة مستفيد حقيقية أو إيقاف الدور مؤقتاً من التسجيل |
| P1 | أزرار دورة الطلب في واجهة المورّد/المندوب مطابقة للـ API + سجل زمني |
| P1 | صندوق إجراءات معلّقة للمشرف |
| P1 | إظهار قائمة المسموحين عند الإسناد في الواجهة |
| P2 | تقليل الأصفار / حالات فارغة موجَّهة بإجراء |
| P2 | `data-testid` لنموذج الدخول ورحلات الكفالة |
| P2 | توحيد مسار API العام للكفالات |
| P3 | ربط توثيق المندوب بالخريطة |
| P3 | بذرة بيانات كادر للتجربة |

---

## 8) أدلة الاختبار (لقطات)

المجلد: `/opt/cursor/artifacts/uat-report/`

| ملف | المحتوى |
|-----|---------|
| `01-anon-home.png` | الرئيسية كزائر |
| `02-anon-signin.png` | صفحة الدخول |
| `03-admin-home.png` | لوحة المشرف |
| `04-admin-sponsorships.png` | إدارة كفالات السقيا |
| `05-donor-portal.png` | بوابة المتبرّع |
| `06-supplier-portal.png` | بوابة المورّد |
| `07-rep-portal.png` | بوابة المندوب |
| `08-volunteer-main.png` | لوحة المتطوع |
| `09-beneficiary-main.png` | المستفيد يُعرض كمتطوع |
| `10-manager-staff.png` | شاشة الكادر للمدير |

مصادر إضافية: `uat3-walk-findings.json`, `uat3-lifecycle-notes.json`.

---

## 9) الخلاصة

المنصة بعد الدمج **قادرة تشغيلياً** على إكمال دورة الكفالة من الطرف إلى الطرف على مستوى الخادم، ولوحات الأدوار الأساسية للكفالات (متبرّع/مورّد/مندوب/مشرف) موجودة. أكبر فجوة منتج هي **تجربة ما بعد الدخول والهوية**: معظم الأدوار تُسقَط في قالب المتطوع، والمستفيد بلا رحلة. النشر على الإنتاج لم يُنفَّذ من هذه البيئة ويتطلّب تشغيل سكربت النشر على الخادم بعد سحب `main`.
