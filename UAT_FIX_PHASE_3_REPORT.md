# UAT_FIX_PHASE_3_REPORT — توحيد نظام الطلبات الديناميكي

**الفرع:** `cursor/feat-dynamic-requests-069a` (مقابل الوثيقة: `feat/dynamic-requests-unified`)  
**الأساس:** طرف المرحلة 2  
**القرار:** D-50

## القرار

النماذج الديناميكية هي النظام الإداري الواحد. الجداول القديمة تبقى (تراكمية) وتُنسخ إلى `RequestSubmission`. الكفالات ليست نموذجاً.

## ما نُفّذ

- `services/legacy_forms.py`: بذرة 3 نماذج نظامية + مرآة + عكس.
- هجرة `services.0004_seed_system_request_forms` قابلة للعكس.
- المسارات العامة القديمة تُراكم مرآة عند الإنشاء.
- الشريط: رابط واحد «النماذج والإرسالات»؛ تحويل المسارات الثابتة.
- اختبارات: backfill، خصوصية العامة، CRUD، عكس.

## جدول متطلب → ملف

| المتطلب | الملف |
|---------|-------|
| بذرة + نسخ | `legacy_forms.py`, `migrations/0004_*` |
| مرآة الكتابة | `services/views.py` public_* |
| واجهة موحّدة | `domains.ts`, `App.tsx`, `RequestFormsAdmin` |
| خصوصية | `tests_unified_requests.py` |
| القرار | D-50 |

## بوابة القبول

| البوابة | النتيجة |
|---------|---------|
| Django | 244 OK |
| vitest | 63/63 |
| build | vendor-react 230.96 kB |
| `check --deploy` | تحذيرات DEBUG فقط |

## مؤجّل

- تحويل صفحات الإدخال العامة (`/request-service` وغيرها) بالكامل إلى `/forms/:slug` لاحقاً (المسارات القديمة ما زالت تعمل مع المرآة).
- دورة الكفالات التشغيلية خارج النطاق.
