# CLARIFICATION_UX3B — محاسبة جولة UAT 3 + فجوة الإصلاح

**التاريخ:** 2026-09-08  
**الفرع:** `cursor/ux3b-admin-no-remount-069a`

## مصدر «آخر تقرير UAT (الجولة 3)»

| مصدر | موجود؟ | ملاحظات |
|------|--------|---------|
| `UAT.md` أعمدة التقييم/ملاحظات مملوءة بـ ⚠️/❌ | **لا** | القالب فارغ؛ النموذج التفاعلي يحفظ في `localStorage` فقط |
| تصدير محفوظ في المستودع لحالة `/uat` | **لا** | لا ملف warn/fail مُصدَّر للجولة 3 |
| خطة الإصلاح الرسمية للجولة 3 | **نعم** | خطة UX3 + تقارير `UX3_PHASE_*` |
| شكاوى الجلسة الحية (بعد دمج UX3) | **نعم** | إعادة تحميل عند «التالي» / حفظ أداة / حفظ إعدادات |

**الخلاصة:** لا توجد قائمة ⚠️/❌ مُصدَّرة من نموذج UAT للجولة 3 داخل المستودع. المحاسبة أدناه = بنود خطة UX3 + NON-ISSUES + أعطال الجلسة الحية، مع إثبات ملف:سطر.

## بنود الجولة 3 — الحالة بعد UX3 وقبل UX3B

| # | البند | الحالة قبل UX3B | إثبات | لماذا لم يُغلق بالكامل؟ |
|---|--------|-----------------|-------|-------------------------|
| A | إعادة تركيب الصفحة عند الحفظ في الإدارة | **جزئي** | `loadMode.ts` موجود؛ مشاريع/نماذج/خرائط تستخدم silent؛ لكن `PlatformProjects.tsx` يربط `load` بـ `[toast]` ثم `useEffect([load])` فيعيد `initial` بعد كل توست | نطاق المرحلة 1 كان ملفات مسمّاة؛ لم يُعالج عدم استقرار كائن التوست |
| B | معالج المشروع بدون إعادة تحميل بين الخطوات | **جزئي** | المعالج وزر التالي `type="button"` موجودان؛ سلسلة التوست تعيد بوابة التحميل وتُغلق النافذة | عُولج كـ UI دون إصلاح `toast→load` |
| C | إزالة بطاقة فهرس الكفالات + زر صفّي | **مُصلَح** | زر صفّي عند تفعيل الأداة في قائمة المشاريع | — |
| D | ربط النماذج + تحرير + QR | **مُصلَح** | `RequestFormsAdmin` + المرحلة 4 | — |
| E | أدوار موحّدة + حذف خرائط | **جزئي للأدوار** | `RolesAdmin` يعرض قدرات+دخول؛ `load` ما زال `setLoading(true)` دائماً | التبديل يحدّث محلياً فبدا يعمل؛ النمط غير موحّد |
| F | محتوى الرئيسية/من نحن | **مُصلَح** | بذرة صفحات ثابتة + Home/About | — |
| G | UAT 3.8 نطاق الكادر تحت التقارير | **متعمد غير مُعالَج** | `UX3_PHASE_6_REPORT.md` NON-ISSUES | قُرئ كـ NON-ISSUE لا مهمة |
| H | UAT 8.1 تفضيلات إشعار المتطوّع | **متعمد غير مُعالَج** | نفس المصدر | قُرئ كـ NON-ISSUE لا مهمة |
| I | صفحات بلا silent (بث/إعدادات/اقتراحات/كفالات/تقارير/طلبات/متطوعون/سقيا/بوابة سقيا) | **غير مُعالَج / متباين** | مثلاً `VolunteersAdmin` يقلب تحميل القائمة بعد الطفرة؛ `AdminPortal`/`SponsorshipTypesPanel` كذلك | خارج قائمة الملفات المسمّاة في المرحلة 1 |
| J | حفظ إعدادات المنصّة يبدو أنه يعيد التحميل | **جزئي** | الحفظ نفسه لا يقلب `loading`؛ التوست غير المستقر يحرّك صفحات أخرى؛ early-return عند التحميل الأولي فقط | خلط بين حفظ الإعدادات وإعادة تركيب صفحات أخرى |

## نمط الفشل (للمنع)

1. نطاق ملفات مسمّاة بدل قاعدة «كل صفحات الإدارة + البوابات المضمّنة».
2. `silent` على الطفرة دون منع إعادة تشغيل `useEffect([load])` عند تغيّر مرجع التوست.
3. كائن Context للتوست جديد كل رسم.
4. استثناءات بلا حارس مستودع.
5. لا اختبار حارس يفشل عند `setLoading(true)` خارج `shouldFlipPageLoading`.

## ماذا يفعل UX3B

- تثبيت `ToastContext` بـ `useMemo`.
- تثبيت `load` (مرجع توست / mount-once).
- تعميم `AdminLoadMode` + silent بعد كل طفرة في كل الإدارة + AdminPortal + SponsorshipTypesPanel.
- حارس Vitest على المجلد بالكامل.
- عدم المساس بـ NON-ISSUES 3.8 و 8.1.

---

## حالة ما بعد UX3B

| # | بعد UX3B |
|---|----------|
| A | **مُصلَح** — توست مُثبَّت + `load` مستقر في المشاريع؛ حارس المستودع أخضر |
| B | **مُصلَح** — التالي لا يعيد `initial` |
| C–F | كما سبق (مُصلَح سابقاً) |
| G–H | ما زالا NON-ISSUE متعمّدان |
| I | **مُصلَح** — Volunteers/Roles/AdminPortal/SponsorshipTypesPanel/ReportGateway |
| J | **مُصلَح** — لا بوابة تحميل عند حفظ الإعدادات؛ توست لا يكسر الصفحات الأخرى |
