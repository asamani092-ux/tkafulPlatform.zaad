# REPO_AUDIT — فحص شامل للمنصّة والمستودع

**التاريخ:** 2026-08-26 · **الفرع:** `feat/consistency-sweep` (رأس مكدّس المراحل الأربع)  
**النطاق:** المنصّة كاملة بعد «الإكمال النهائي للأدوات» (دورة الحياة، النوع، إعدادات الأدوات، الاتساق).

هذا تقرير ملاحظات وفجوات، وليس قائمة أعطال حرجة. البوّابات كلها خضراء (انظر «الحالة»).

## 1) الحالة العامة (البوّابات)

| البوّابة | النتيجة |
|---------|---------|
| اختبارات الخادم | **226** OK (skipped=1) |
| `check --deploy` | نظيف — 0 مشاكل |
| اختبارات الواجهة (vitest) | **48** ناجحة (13 ملفاً) |
| `tsc && vite build` | ناجح؛ أكبر chunk `vendor-react` **230.96 kB** < 250KB |
| بذور/هجرات المشروع | `0005_project_type` قابلة للعكس (forward→reverse→forward، 5 أنواع) |

## 2) بنية المستودع

- Monorepo: `backend/` (Django REST)، `frontend/` (React+Vite+TS)، `design-system/` (CSS/توكنز).
- Backend apps: `accounts, core, projects, maps, sponsorships, services, reporting, analytics, volunteering, notifications, integrations` + توافقات قديمة (`takaful_app, saqya, impact_map`).
- طبقة الأدمن الأفقية (مستخدمون/إعدادات/إشعارات/أدوار/سجل نشاط) داخل `accounts` + `core` + `notifications`.

## 3) ملاحظات وفجوات (مرتّبة بالأولوية)

### (أ) اعتماد الدمج — مكدّس فوق `feat/activity-log` وليس `main`  ‹متوسطة›
- المراحل الأربع (وطبقة الأدمن الخمس قبلها) **غير مدموجة في `main`**. سلاسل الفروع:
  `feat/admin-user-management → … → feat/activity-log → feat/project-lifecycle → feat/project-type → feat/tool-config → feat/consistency-sweep`.
- **الأثر**: `main` لا يحتوي بعد على أيٍّ من هذا العمل. يجب دمج المكدّسين بالترتيب قبل UAT.
- موثّق في **D-43**.

### (ب) هجرتا إعادة تسمية فهارس مفقودتان (تجميلية)  ‹منخفضة›
- `makemigrations --check` يطلب:
  - `maps`: إعادة تسمية 3 فهارس (سابقة لهذا العمل — تحذير معروف).
  - `notifications`: إعادة تسمية فهرس `NotificationPreference` (اسم صريح في هجرة `0002` مقابل اسم تلقائي في الموديل — من طبقة الإشعارات).
- **لا أثر على البيانات ولا على `check --deploy`** (يبقى نظيفاً). لم تُولَّد الهجرتان تفادياً للمسّ بهجرة `maps` المحذّر منها ولتقليل الضجيج.
- **توصية**: توليد هجرتَي إعادة التسمية لاحقاً لجعل `makemigrations --check` نظيفاً.

### (ج) صفحات قوائم قديمة بلا `ErrorState` مخصّص  ‹منخفضة›
- `ServiceRequests`, `ProjectIdeas`, `VolunteerManagement/Applications/Requests` تبتلع أخطاء الجلب
  (`.catch(() => {})`) وتعرض empty-state + toast بدل لوحة خطأ مصمّمة.
- **الأثر**: عند فشل الشبكة تظهر «لا توجد بيانات» بدل رسالة خطأ. مقبول وظيفياً؛ الصفحات الجديدة
  (أنواع/أدوار/سجل/بث) تستخدم `PageStates` كاملة. موثّق في **D-46**.

### (د) تحذير Circular chunk في البناء  ‹منخفضة›
- `Circular chunk: projects -> admin -> projects` أثناء `vite build` — تحذير تقسيم فقط، البناء ينجح
  والأحجام ضمن البوّابة. لا حاجة لإجراء عاجل.

### (هـ) ازدواج إشعارات الكفالات (saqya القديمة + المركز الجديد)  ‹منخفضة›
- `sponsorships/notifications.py` القديمة ما زالت تُنشئ `Notification` (+ بريد اختياري) على الإسناد/الجهوزية،
  بالتوازي مع `notifications.services.notify` الجديد. مقصود ومقبول؛ التوحيد لاحقاً اختياري.

## 4) أمن وخصوصية (مؤكّد)

- كل كتابة إدارية جديدة `IsAdmin`/super-admin؛ غير المشرف 403 (`core.tests_consistency`).
- القراءات العامة الجديدة (`public/project-types`, حمولات المشاريع) حقول آمنة فقط، بلا PII (اختبارات).
- الخصوصية (PDPL): إخفاء `<5` وتجميعات في الخرائط سليمة؛ لا تسريب في النقاط العامة.
- الظهور العام = النشطة فقط عبر المشاريع والخرائط (D-43).

## 5) توصيات ما قبل UAT

1. دمج المكدّسين بالترتيب إلى `main` (طبقة الأدمن ثم الإكمال النهائي).
2. توليد هجرتَي إعادة تسمية الفهارس (maps + notifications) لتنظيف `makemigrations --check`.
3. (اختياري) ترقية صفحات القوائم القديمة إلى `ErrorState` عند فشل الجلب.
4. مراجعة `FEATURE_MATRIX.md` كخريطة لجولة `/uat`.

## 6) الخلاصة

المنصّة مكتملة وظيفياً ومتّسقة للأدوات المستهدفة، وكل البوّابات خضراء. الفجوات المتبقّية تجميلية
أو تنظيمية (دمج/هجرات إعادة تسمية/لوحات خطأ)، بلا مخاطر على البيانات أو الأمن.
