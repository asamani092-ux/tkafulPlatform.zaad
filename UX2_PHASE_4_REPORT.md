# UX2 — تقرير المرحلة 4: إكمال نطاقات الإدارة (RC-D)

**الفرع:** `fix/admin-domains-v2` · **الأساس:** `fix/role-vocab-staff`

كل النطاقات تستخدم مكوّنات نظام التصميم، بحقول معنونة وسلوك العميل المطلوب.

## المشاريع (3.3)
- **إضافة الأعضاء = بحث متعدّد (MultiSelect) على كل مستخدمي المنصّة** بلا قيد دور
  (قرار العميل)؛ يُحمّل `/api/accounts/users/?page_size=100` مرّة، بحث محلي O(n)،
  ويُضاف المختارون بالدور المحدّد (قابل للتعديل لاحقاً). أُزيل إدخال «معرّف المستخدم».
- **لوح الأدوات**: تنبيه إرشادي يشرح التفعيل/الإعدادات؛ إعداد الأداة عبر `Textarea` معنون.
- بلا إعادة تحميل كامل (يعتمد على المرحلة 2).

## المتطوّعون (3.4)
- **تقرير الإنجاز لكل متطوّع** يعرض المقاييس المطلوبة:
  **ساعات التطوّع + عدد المشاريع المشارَك فيها + المهام المنجزة** (+ الحالية).
- الخلفية: `/api/reports/volunteer-tasks/` يُرجِع الآن `metrics`.

## الطلبات (3.5)
- منشئ النموذج: كل حقل يملك **تسمية عربية + نوع + نص إرشادي (placeholder)**.
- **معاينة حيّة** لنموذج المستفيد بجانب المنشئ (عرض فقط، حقول معطّلة).

## الكفالات (3.6)
- إصلاح السلوك القائم دون بناء بنية جديدة: البوابة داخل المشروع صارت **مُنطّقة
  بالمشروع** — تمرير `?project=<slug>` إلى `dashboard/sponsorships/orders`
  (خلفية: تصفية querysets). أزرار الاعتماد/الرفض/الإسناد موجودة وتعمل؛ قوائم
  الإسناد أصبحت **معنونة**. الخروج التلقائي عولج في المرحلة 2.

## الخرائط (3.7)
- **إضافة أسهل + رفع بالجملة**: نقطة `POST /api/maps/admin/items/bulk_upload/`
  (CSV/JSON) + **قالب قابل للتنزيل** `GET .../items/template/`.
- **تحليل إحداثيات متساهل** (`maps/coordinates.py`): يقبل روابط Google
  (`@lat,lng`, `?q=`, `!3d!4d`)، `geo:`، `lat=..&lng=..`، وخام «lat, lng»؛
  يطبّع ويتحقق من نطاق WGS-84 على الخادم.
- **زر «الذهاب للموقع»** لكل عنصر يفتح الإحداثيات في تطبيقات الخرائط الخارجية.
- الحقول المخصّصة لكل موقع عبر `MapItemField` القائم؛ النشر بالتفاصيل أو دونها.

## التقارير (3.9)
- **بوّابة تقارير موحّدة** (`ReportGateway`) بنطاق قابل للاختيار:
  **المنصّة / مشروع محدّد / المتطوّعون / الكفالات** — خلفية `/api/reports/scope/`.
- **عرض على الشاشة + تصدير CSV** (‏`utils/csv.ts`) **+ طباعة PDF عربية** عبر
  محرّك المتصفّح (تشكيل عربي صحيح، RTL) بدل مسار PDF يشوّه العربية.

## البوّابة (Gate)
- ✅ الخلفية: 29 اختبار في `maps/reporting/sponsorships` `OK`
  (محلّل الإحداثيات، الرفع بالجملة، نطاق التقارير، مقاييس المتطوّع، تصفية الكفالات).
- ✅ Vitest: **88/88** (حارس الحقول المعنونة يشمل `aria-label`).
- ✅ `tsc && vite build` أخضر.
