# خطة تنفيذ نظام ملف المشروع

قائمة خطوات مرقّمة بمربعات تحقق. يُحدَّث أثناء التنفيذ.

## 1. بنية البيانات

- [x] تطبيق `backend/projectdocs/` مسجّل في `INSTALLED_APPS`
- [x] `ProjectDossier` (OneToOne مع Project) + رمز داخلي فريد
- [x] `DossierSection` (document / closure) + حالة القسم
- [x] `DossierStage` (المراحل الخمس)
- [x] `StageActivity` (الخطة التنفيذية)
- [x] `ApprovalRequest` (توكن وحيد الاستعمال)
- [x] `DossierAttachment` تحت `private/dossiers/`
- [x] هجرة أولية

## 2. كتالوج الأقسام

- [x] 15 قسماً لوثيقة المشروع في `sections.py`
- [x] 11 قسماً لوثيقة الإغلاق
- [x] ربط كل قسم بمرحلته الافتراضية
- [x] تحقق الحقول في الخادم + `schema_payload` للواجهة

## 3. واجهات البرمجة

- [x] `GET/POST /api/projectdocs/dossiers/`
- [x] `GET/PATCH /api/projectdocs/dossiers/{id}/`
- [x] `PATCH .../sections/{kind}/{key}/`
- [x] `POST .../stages/{order}/submit/`
- [x] `CRUD .../activities/`
- [x] `POST .../attachments/`
- [x] `GET /api/projectdocs/schema/`

## 4. الاعتماد بالبريد

- [x] توليد توكن فريد + انتهاء صلاحية
- [x] إرسال بريد للراعي برابط `/approvals/:token`
- [x] `GET/POST /api/public/approvals/{token}/`
- [x] تقييد معدل عبر `AuthRateThrottle`
- [x] استعمال واحد + تسجيل `ActivityLog`

## 5. بوابات المراحل

- [x] قفل المراحل غير النشطة
- [x] فتح المرحلة التالية عند الاعتماد
- [x] إعادة للتعديل مع سبب
- [x] اعتماد داخلي من المشرف عند الحاجة

## 6. الخطة التنفيذية

- [x] أنشطة وأكواد وأنشطة فرعية
- [x] حالات تلقائية حسب التواريخ
- [x] تسليمات المرحلة + شواهد (مرفقات)

## 7. وثيقة الإغلاق

- [x] أقسام الإغلاق بعد المرحلة الخامسة
- [x] قياس النطاق / الزمن / المال / الدروس

## 8. الواجهة الأمامية

- [x] مساحة عمل `/Admin/projects/:slug/dossier`
- [x] تبويبات: البطاقة · الوثيقة · الخطة · الإغلاق · اللوحة
- [x] شريط المراحل
- [x] عارض أقسام من المخطط
- [x] صفحة عامة `/approvals/:token`

## 9. التذكيرات

- [x] أمر `send_project_reminders`
- [x] توثيق تشغيل Cron في كولفاي

## 10. الاختبارات

- [x] اختبارات خادم (أقسام، بوابات، توكن، صلاحيات)
- [x] بناء الواجهة / تحقق الأنواع
- [x] تجربة يدوية مع لقطات وفيديو

## 11. التصدير

- [x] تصدير PDF بهوية الجمعية للوثيقة والإغلاق

## 12. إعادة الهيكلة بعد اجتماع العميل

- [x] إنشاء مشروع بالاسم + الراعي → بطاقة الملف مباشرة (`create_project_with_dossier`)
- [x] بنود تكلفة `BudgetLine` + حركات تراكمية `BudgetTxn` (مقترح / مخصص / صرف)
- [x] فصل الوثيقة عن الإغلاق + مقارنة `GET .../comparison/`
- [x] شاهد إلزامي + درس مستفاد عند إتمام النشاط
- [x] شريط مراحل مع رسالة «اعتماد هذه المرحلة فقط»
- [x] **لا Excel لملف المشروع**: مسار `projectdocs` لا يستورد ولا يصدّر Excel؛ `import_excel_data` وملفات `data_*.xlsx` خاصة بالمتطوعين/المشاريع القديمة فقط وليست مصدراً لحقول الملف. التصدير المعتمد للملف هو PDF فقط.
