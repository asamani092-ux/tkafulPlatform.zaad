# UX2 — تقرير المرحلة 2: جذر المصادقة/التنقل (RC-B)

**الفرع:** `fix/auth-nav-root-cause` · **الأساس:** `feat/adopt-design-system`

## المشكلة الجذرية
`lib/api.ts::authFetch` كان عند أي 401 يجدّد مرة، وعند الفشل يمسح الجلسة ويعيد
تحميل الصفحة كاملة عبر `window.location.href="/signin"`. النتيجة: خروج قسري
بإعادة تحميل (شعور «إعادة تحميل عند كل تغيير» — #4)، وتسجيل خروج تلقائي عند
عودة المشرف من بوابة كفالات المشروع (UAT 3.6).

## ما أُنجز
### قاعدة الحالة الجديدة في `authFetch`
- **401 + نجاح التجديد** → إعادة الطلب بصمت، الجلسة محفوظة.
- **401 + فشل التجديد** (أو غياب refresh) → انتهاء جلسة حقيقي: مسح التخزين
  و`notifySessionExpired()` التي تُنقّل عبر **React Router** لا إعادة تحميل.
- **403** (جلسة صالحة/مورد غير مصرّح) → يُعاد كما هو، **لا يمسّ الجلسة إطلاقاً**.

### جسر انتهاء الجلسة `lib/authEvents.ts`
طبقة الـ API خارج شجرة React؛ يسجّل `AppContent` معالجاً عبر `useEffect`
(`useSessionExpiryRedirect`) يستخدم `useNavigate` للانتقال إلى
`/signin?next=<المسار الحالي>` دون إعادة تحميل، مع تفادي الحلقات على `/signin`.

### العودة للوجهة بعد إعادة الدخول
`SignIn` يقرأ `?next=` (مسارات داخلية آمنة فقط: تبدأ بـ `/` وليست `//`) ويعيد
المستخدم لموضعه بعد الدخول.

### تدقيق التنقّل الداخلي
لا يوجد `window.location.href`/`reload`/`assign`/`replace` لأي تنقّل داخلي في
`src/` (تحويل الدفع الخارجي في `DonorPortal` عبر `redirect_url` مقصود ويبقى).

## البوّابة (Gate)
- ✅ Vitest: **84/84** (منها `api.test.ts`: 403≠خروج · 401+تجديد ناجح=إبقاء ·
  401+فشل=مسح+إبلاغ · 401 بلا refresh=إنهاء).
- ✅ `tsc && vite build` أخضر.
- ✅ حارس: لا `window.location.href` لتنقّل داخلي.

## ملاحظة تحقيقية
لا يوجد في الخلفية أي مسار يُعيد 401 لأسباب صلاحية (DRF يُعيد 403 للمصادَق
الممنوع). فسبب «401 عند العودة» كان انتهاء التوكن أثناء الزيارة ثم إعادة التحميل
القسرية — وقد عولج جذرياً بالتنقّل عبر الراوتر والحفاظ على الجلسة عند 403.
